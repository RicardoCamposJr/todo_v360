<div class="container mt-4 pb-5 responsive-width">
  <div class="content-align">
    <div style="max-width: 500px;">
      <h1 class="h2 mb-2">Editar lista</h1>
      <%= form_with model: @todo_list, html: { class: "needs-validation" } do |form| %>
        <div class="mb-3">
          <%= form.label :title, "Título", class: "form-label" %>
          <%= form.text_field :title, class: "form-control", required: true %>
        </div>
        <div class="d-grid gap-2">
          <%= form.submit 'Atualizar', class: "btn btn-primary" %>
        </div>
      <% end %>
      <div class="mt-3">
        <%= link_to 'Voltar', todo_lists_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>